# Services

This folder is intentionally cleared.

Planned ownership for the rebuild:

- `job discovery`
- `resume parsing`
- `profile building`
- `AI query generation`
- `AI ranking`
- `application tracking`

Current API routes return `501 Not Implemented` until these services are rebuilt.
